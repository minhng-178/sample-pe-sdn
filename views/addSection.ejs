<h2>Add New Section</h2>

<form action="/sections" method="POST">
  <div class="form-group">
    <label for="sectionName">Section Name</label>
    <input
      type="text"
      class="form-control"
      id="sectionName"
      name="sectionName"
      required
    />
    <small class="form-text text-muted">
      Section name should start with a capital letter and can contain letters,
      digits, spaces, and forward slashes.
    </small>
  </div>
  <div class="form-group">
    <label for="sectionDescription">Section Description</label>
    <textarea
      class="form-control"
      id="sectionDescription"
      name="sectionDescription"
      required
    ></textarea>
  </div>
  <div class="form-group">
    <label for="duration">Duration</label>
    <input
      type="number"
      class="form-control"
      id="duration"
      name="duration"
      required
    />
  </div>
  <div class="form-group form-check">
    <div class="form-group">
      <label>Main Task</label><br />
      <input
        type="radio"
        id="isMainTaskTrue"
        name="isMainTask"
        value="true"
        required
      />
      <label for="isMainTaskTrue">True</label><br />
      <input
        type="radio"
        id="isMainTaskFalse"
        name="isMainTask"
        value="false"
      />
      <label for="isMainTaskFalse">False</label><br />
    </div>
  </div>
  <div class="form-group">
    <label for="course">Course</label>
    <select class="form-control" id="course" name="courseId" required>
      <% courses.forEach(course => { %>
      <option value="<%= course._id %>"><%= course.courseName %></option>
      <% }) %>
    </select>
  </div>
  <div class="mt-2">
    <a href="/dashboard" class="btn btn-secondary">Back</a>
    <button type="submit" class="btn btn-bd-primary">Add Section</button>
  </div>
</form>
